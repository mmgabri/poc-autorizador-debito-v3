spring:
  threads:
    virtual:
      enabled=true:

server:
  port: 9097

logging:
  level:
    br.com.mmgabri: error

management:
  metrics:
    distribution:
      percentiles-histogram:
        all: true
  datadog:
    metrics:
      export:
        apiKey: ${DD_API_KEY}
        step: 5s
        uri: https://us5.datadoghq.com/
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus